attribute(internal, doc("Internally used module. Don't use directly."));

use(reflection);

def continuationToString(Continuation this)
(
    target(Continuation),
    name(toString),
)
{
    "Continuation:\n" ~ this.trace
}
