/*
  Check that we can inherit from built in types
*/

type Sub (extends(Int))
{
    def bar(){print("Sub.bar()\n")}
}

def main()
{
    var sub=Sub(7);
    sub.bar();
    print(sub; sub+1; "\n";); 
}