/*
  Basic inheritance test
*/

class Sub (extends(Parent))
{
    def bar(){print("Sub.bar()")}
}

class Parent
{
    def foo(){print("Parent.foo()")}
}

def main()
{
    sub:=Sub();
    sub.foo();
    sub.bar();
    print(sub);
}