
def main()
    (doc("A few tests for the FdSet"))
{
    use(unix.io);

    fs := FdSet();
    fs2 := FdSet();
    fs3 := FdSet();

    fs.zero();
    fs2.zero();
    fs3.zero();

    [3,5,8].each(idx){fs.set(idx)}
    (0..10).each(idx){print("Fd %: %" % [ idx, fs.set?(idx)])}
}

