def main()
{
    var mm = HashMap();
    mm[4] = 'g';
    mm["oyster"] = 1.234;
    print(mm, '\n');

    /* Vowel stripping filter */
    print("HEY baberiba!\n".filter(ch){! ("aoueiy".search(ch.lower))});
}
